# Results

This folder holds analytic outputs (tables, model coefficients, summary CSVs).

**Contents are gitignored** because they may contain PHI or derived patient-level information. To regenerate, run the notebooks in `../notebooks/` in order.
